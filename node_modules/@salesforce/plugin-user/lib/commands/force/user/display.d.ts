import { SfdxCommand } from '@salesforce/command';
import { Logger } from '@salesforce/core';
export declare type UserDisplayResult = {
    username: string;
    profileName: string;
    id: string;
    orgId: string;
    accessToken: string;
    instanceUrl: string;
    loginUrl: string;
    alias?: string;
    password?: string;
};
export declare class UserDisplayCommand extends SfdxCommand {
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresUsername = true;
    static readonly requiresDevhubUsername = true;
    logger: Logger;
    run(): Promise<UserDisplayResult>;
    private print;
}
