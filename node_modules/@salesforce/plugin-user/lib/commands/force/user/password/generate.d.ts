import { FlagsConfig, SfdxCommand } from '@salesforce/command';
import { Org } from '@salesforce/core';
interface PasswordData {
    username?: string;
    password: string;
}
export declare class UserPasswordGenerateCommand extends SfdxCommand {
    static readonly description: string;
    static readonly examples: string[];
    static readonly requiresUsername = true;
    static readonly requiresDevhubUsername = true;
    static readonly flagsConfig: FlagsConfig;
    org: Org;
    private usernames;
    private passwordData;
    run(): Promise<PasswordData[] | PasswordData>;
    private print;
}
export {};
