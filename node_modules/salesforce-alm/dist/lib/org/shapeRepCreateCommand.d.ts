import VarargsCommand from '../core/varargsCommand';
interface ShapeResult {
    shapeId: string;
    shapeFile: string;
    success: boolean;
    errors: [];
}
declare class ShapeRepCreateCommand extends VarargsCommand {
    private shapeApi;
    constructor();
    /**
     * secondary validation from the cli interface. this is a protocol style function intended to be represented by other
     * commands
     *
     * @param context - this cli context
     * @returns {Promise}
     */
    validate(context: any): Promise<any>;
    execute(): Promise<ShapeResult>;
    getHumanSuccessMessage(shape: any): string;
}
export = ShapeRepCreateCommand;
